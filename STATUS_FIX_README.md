# 项目状态显示问题修复说明

## 问题描述
"单位项目管理"页面的"当前服务状态"列大多显示"草稿"，这是因为合同创建时使用了模型中的默认状态值。

## 问题原因

### 1. 后端问题
- 合同模型中的 `status` 字段默认值为 `"草稿"`
- 合同创建时没有明确设置状态，导致使用默认值
- 缺乏状态转换逻辑

### 2. 前端问题
- 状态显示不够友好
- 缺乏状态标签和颜色区分
- 没有状态映射转换

## 解决方案

### 1. 后端修复

#### 修改合同创建逻辑
- 新创建的合同默认状态设为 `"已审核"`（服务中）
- 位置：`controllers/contract.controller.js`

```javascript
// 在合同创建时设置合适的状态
status: "已审核", // 新创建的合同默认为已审核状态
```

#### 状态定义
- **草稿**：待处理
- **已提交**：待审核
- **已审核**：服务中
- **已归档**：已完成
- **已续签**：已续签

### 2. 前端优化

#### 状态显示优化
- 添加状态标签和颜色
- 实现状态文本映射
- 提供更友好的显示

#### 状态映射
```javascript
// 状态标签颜色
'草稿': 'info'      // 灰色
'已提交': 'warning'  // 橙色
'已审核': 'success'  // 绿色
'已归档': 'primary'  // 蓝色
'已续签': 'success'  // 绿色

// 状态显示文本
'草稿': '待处理'
'已提交': '待审核'
'已审核': '服务中'
'已归档': '已完成'
'已续签': '已续签'
```

## 修复内容

### 1. 后端修改
- ✅ 修改合同创建逻辑，设置默认状态为"已审核"
- ✅ 保持现有的状态转换逻辑（提交、审核等）

### 2. 前端修改
- ✅ 添加状态标签显示
- ✅ 实现状态颜色映射
- ✅ 添加状态文本转换
- ✅ 优化用户界面体验

### 3. 测试脚本
- ✅ 创建状态更新测试脚本
- ✅ 提供状态分布统计
- ✅ 支持批量状态更新

## 使用方法

### 1. 运行测试脚本
```bash
cd FireSafe_backend
node test_status_update.js
```

### 2. 查看效果
- 启动前端和后端服务
- 访问"单位项目管理"页面
- 查看状态列的显示效果

## 状态流转逻辑

### 正常流程
1. **草稿** → 初始状态
2. **已提交** → 用户提交审核
3. **已审核** → 审核通过，开始服务
4. **已归档** → 服务完成
5. **已续签** → 合同续签

### 审核流程
- **已提交** → **已审核**（审核通过）
- **已提交** → **草稿**（审核不通过）

## 预期效果

### 修复前
- 大部分项目显示"草稿"
- 状态显示单调
- 用户体验差

### 修复后
- 新项目默认显示"服务中"
- 状态标签颜色丰富
- 显示文本更友好
- 用户体验提升

## 注意事项

1. **数据迁移**：现有数据需要手动更新状态
2. **权限控制**：确保只有有权限的用户可以修改状态
3. **状态一致性**：确保前后端状态定义一致
4. **用户体验**：状态显示要直观易懂

## 后续优化

### 1. 可扩展功能
- 状态变更历史记录
- 状态变更通知
- 状态统计报表
- 自动状态转换

### 2. 可优化项目
- 状态变更工作流
- 状态权限细分
- 状态模板配置
- 状态变更审计

## 总结

通过这次修复，解决了项目状态显示的问题：

1. ✅ 修复了后端默认状态设置
2. ✅ 优化了前端状态显示
3. ✅ 提供了测试和验证工具
4. ✅ 改善了用户体验
5. ✅ 保持了系统一致性

现在项目状态显示将更加合理和友好！ 