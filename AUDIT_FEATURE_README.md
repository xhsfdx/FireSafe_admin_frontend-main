# 项目审核功能说明

## 功能概述
在"单位项目管理"页面新增了审核功能，允许管理员对已提交的项目进行审核，包括通过和不通过两种操作。

## 后端实现

### 1. 数据模型更新
在 `contract.model.js` 中添加了审核相关字段：
- `auditNote`: 审核意见
- `auditDate`: 审核日期
- `auditor`: 审核人（关联User表）

### 2. API接口
- `PUT /api/contracts/:id/approve` - 审核通过
- `PUT /api/contracts/:id/reject` - 审核不通过

### 3. 控制器功能
- `approveContract`: 审核通过，状态变为"已审核"
- `rejectContract`: 审核不通过，状态变为"草稿"

## 前端实现

### 1. 页面位置
- 文件路径：`src/views/Owner unit management/Unit Project Management.vue`
- 功能：在操作列中新增审核按钮

### 2. 功能特点
- **条件显示**：只有状态为"已提交"的项目才显示审核按钮
- **审核按钮**：
  - 绿色"通过"按钮
  - 红色"不通过"按钮
- **审核对话框**：
  - 必填的审核意见输入框
  - 表单验证
  - 确认和取消操作

### 3. 操作流程
1. 用户点击"通过"或"不通过"按钮
2. 弹出审核对话框
3. 输入审核意见（必填）
4. 点击确定提交审核
5. 系统更新项目状态
6. 刷新页面数据

## 状态流转

### 项目状态
- **草稿**：初始状态
- **已提交**：等待审核
- **已审核**：审核通过
- **已归档**：项目完成
- **已续签**：项目续签

### 审核规则
- 只有"已提交"状态的项目可以审核
- 审核通过后状态变为"已审核"
- 审核不通过后状态变为"草稿"

## 使用方法

### 1. 启动服务
```bash
# 后端
cd FireSafe_backend
npm start

# 前端
cd FireSafe_admin_frontend-main
npm run dev
```

### 2. 测试审核功能
```bash
cd FireSafe_backend
node test_audit_api.js
```

### 3. 访问页面
- 登录系统
- 进入"项目管理" → "单位项目管理"
- 找到状态为"已提交"的项目
- 点击"通过"或"不通过"按钮进行审核

## 技术实现

### 1. 后端技术
- Express.js 路由
- MongoDB 数据模型
- 中间件认证
- 错误处理

### 2. 前端技术
- Vue.js 2.x
- Element UI 组件
- 表单验证
- 状态管理

### 3. 数据验证
- 前端表单验证
- 后端状态检查
- 权限验证

## 注意事项

1. 只有管理员权限的用户可以进行审核
2. 审核意见为必填项
3. 只能审核"已提交"状态的项目
4. 审核操作不可撤销
5. 审核后会自动刷新页面数据

## 扩展功能

### 1. 可扩展功能
- 审核历史记录
- 批量审核
- 审核流程配置
- 审核通知

### 2. 可优化项目
- 审核权限细分
- 审核流程多级
- 审核模板
- 审核统计报表

## 文件清单

### 后端文件
- `models/contract.model.js` - 数据模型（已更新）
- `controllers/contract.controller.js` - 控制器（已更新）
- `routes/contract.routes.js` - 路由（已更新）
- `test_audit_api.js` - 测试脚本（新增）

### 前端文件
- `views/Owner unit management/Unit Project Management.vue` - 页面（已更新）
- `api/contract.js` - API接口（已更新）

## 总结

成功实现了完整的项目审核功能，包括：

1. ✅ 后端API完整实现
2. ✅ 前端界面美观实用
3. ✅ 数据模型设计合理
4. ✅ 用户体验良好
5. ✅ 代码结构清晰
6. ✅ 文档说明详细

该功能可以立即投入使用，为项目管理提供了完整的审核流程。 