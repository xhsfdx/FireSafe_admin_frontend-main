# 结款管理功能说明

## 功能概述
新增了结款管理功能，用于跟踪和管理项目合同的结款状态。

## 后端功能

### 1. 数据模型更新
- 在 `contract.model.js` 中添加了结款相关字段：
  - `paymentStatus`: 结款状态（未结款/部分结款/已结款）
  - `paidAmount`: 已结金额
  - `paymentDate`: 结款日期
  - `paymentMethod`: 结款方式（现金/银行转账/支票/其他）
  - `paymentNote`: 结款备注

### 2. API接口
- `GET /api/payment/list` - 获取结款列表
- `PUT /api/payment/:contractId/status` - 更新结款状态
- `GET /api/payment/stats` - 获取结款统计信息

### 3. 控制器
- `payment.controller.js` - 处理结款相关的业务逻辑

### 4. 路由
- `payment.routes.js` - 定义结款相关的路由

## 前端功能

### 1. 页面位置
- 文件路径：`src/views/Owner unit management/Payment Management.vue`
- 路由路径：`/owner/payment`
- 菜单名称：结款管理

### 2. 主要功能
- **查询筛选**：支持按业主单位、委托单位、结款状态、合同种类筛选
- **数据展示**：显示合同金额、已结金额、未结金额、结款状态等信息
- **状态更新**：可以更新单个项目的结款状态
- **金额统计**：支持统计选中项目或全部项目的金额
- **分页显示**：支持分页浏览数据

### 3. 操作功能
- **查看详情**：跳转到项目详情页面
- **更新结款**：打开对话框更新结款信息
- **结款记录**：查看结款历史记录（功能开发中）
- **批量更新**：批量更新多个项目的结款状态（功能开发中）

## 使用方法

### 1. 启动后端服务
```bash
cd FireSafe_backend
npm install
npm start
```

### 2. 启动前端服务
```bash
cd FireSafe_admin_frontend-main
npm install
npm run dev
```

### 3. 访问页面
- 登录系统后，在左侧菜单找到"项目管理"
- 点击"结款管理"进入页面

### 4. 测试API
```bash
cd FireSafe_backend
node test_payment_api.js
```

## 数据字段说明

### 结款状态
- **未结款**：项目尚未收到任何付款
- **部分结款**：项目已收到部分付款
- **已结款**：项目已收到全部付款

### 结款方式
- **现金**：现金支付
- **银行转账**：银行转账支付
- **支票**：支票支付
- **其他**：其他支付方式

## 注意事项

1. 结款金额不能超过合同总金额
2. 更新结款状态时会自动计算未结金额
3. 结款日期为必填项
4. 支持按多种条件筛选和查询
5. 数据实时更新，无需刷新页面

## 后续开发计划

1. 完善结款记录功能
2. 添加批量更新功能
3. 增加结款报表导出
4. 添加结款提醒功能
5. 优化数据统计展示 